# xenatra - Managing instant XEN DomU clones with lvm snapshots 

## prerequisites
* XEN hypervisor on a Linux host (no XenServer or XCP)
* LVM as DomU storage


Twitter Bootstrap UI Concept only so far...

install with ruby 1.9.3 and 

    bundle install

run t with

    bundle exec unicorn



